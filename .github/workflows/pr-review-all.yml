name: "On PR REVIEW"

on:
  pull_request_review:

jobs:
  approved:
    if: github.event.review.state == 'approved'
    runs-on: ubuntu-latest
    steps:
      - run: echo "This PR was approved"

  always:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This PR was reviewed"
